//num1을 2로나눈  나머지를 구하시오 출력하시오 

#include <stdio.h>
 
int main()
{
    int num1 = 7;
 
    num1 %= 2;    // num1에서 2를 나눈 뒤 나머지를 구하여 다시 num1에 저장
 
    printf("%d\n", num1);    // 1
 
    return 0;
}

/* ☆☆☆☆☆☆☆☆☆☆☆☆☆☆  C99 표준에서 나머지 연산자는 a == (a / b) * b + a % b로 정의하고 있습니다. 아래의 예제를 참고합시다.
   5 == 5 / (-3) * (-3) + 5 % (-3) // 5 / (-3)은 (-1) x 5 / 3 ==> (-1) {5/3은 몫이 1이기 때문입니다!}
   5 == (-1) * (-3) + 2 // 나머지 연산은 스스로 될거라 생각합니다.
   5 == 3 + 2

결론적으로 a % b를 연산하면 결과는 a의 부호를 따릅니다(b의 부호는 무시). */
